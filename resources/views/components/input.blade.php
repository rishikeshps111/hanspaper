<input type="{{ $type }}" id="{{$id}}" name="{{ $name }}" value="{{ $value }}" {{ $required ? 'required' : '' }} class="form-control {{ $additionalClasses }}" autocomplete='{{ $autocomplete ? "true" : "false" }}' placeholder="{{ $placeholder }}" {{ $disabled ? 'disabled' : '' }} {{ $autofocus }} {{ $readonly ? 'readonly' : '' }} minlength="{{ $minlength }}" minlength="{{ $minlength }}" maxlength="{{ $maxlength }}" />
